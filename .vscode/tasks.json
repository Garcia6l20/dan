{
    "tasks": [
        {
            "taskName": "makeTestDirs",
            "command": "mkdir",
            "args": [
                "-p", "${workspaceFolder}/build/tests/simple", "${workspaceFolder}/build/tests/cxx/simple"
            ],
            "isShellCommand": true
        }
    ]
}