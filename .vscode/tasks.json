{
    "tasks": [
        {
            "taskName": "makeTestDirs",
            "command": "mkdir",
            "args": [
                "-p", "${workspaceFolder}/build/tests/simple"
            ],
            "isShellCommand": true
        }
    ]
}